spring:
  application:
    name: licensingservice
  profiles:
    active: default
  cloud:
    config:
      enabled: true
      #uri: http://configserver:10000
#      discovery:
#        enabled: true
#        serviceId: configserver